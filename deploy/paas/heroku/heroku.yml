build:
  docker:
    web: packages/backend/Dockerfile
  config:
    DOCKER_BUILDKIT: 1
run:
  web: sh -c 'python -m flask --app wsgi:app init-db && gunicorn --workers=2 --threads=4 --bind 0.0.0.0:$PORT wsgi:app'
